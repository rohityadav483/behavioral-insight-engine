# ── Groq API ──────────────────────────────────────────────────────────────────
# Get key at: https://console.groq.com
# Required for AI narration. System runs without it — raw descriptions used instead.
GROQ_API_KEY=your_groq_api_key_here